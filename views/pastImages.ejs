<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Past Image</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <h1 class="display-1">Nasa Past Images</h1>
    <a href="/" class="btn btn-primary mb-4">Back to Home</a>
    <form action="/pastImage" method="POST" class="mt-4 container mx-auto">
        <div class="mb-3 container">
            <label for="pastDate" class="form-label">Select a date:</label>
            <input type="date" id="pastDate" name="pastDate" class="form-control" required>
        </div>
        <button type="submit" class="btn btn-primary">Fetch Image</button>
    </form>

    <% if (data != null) { %>
        <div class="container mt-5">
            <div class="card mx-auto" style="max-width: 600px;">
                <img src="<%= data.url %>" class="card-img-top" alt="NPOD">
                <div class="card-body">
                    <h5 class="card-title"><%= data.title %></h5>
                    <p class="card-text"><%= data.explanation %></p>
                    <p class="card-text"><small class="text-muted"><strong>Date:</strong> <%= data.date %></small></p>
                </div>
            </div>
            <form action="/like-image" method="POST">
                <input type="hidden" name="url" value="<%= data.url %>">
                <input type="hidden" name="title" value="<%= data.title %>">
                <input type="hidden" name="explanation" value="<%= data.explanation %>">
                <input type="hidden" name="date" value="<%= data.date %>">
                <button type="submit" class="btn btn-primary">Like</button>
            </form>
        </div>
    <% } %>
</body>
</html>